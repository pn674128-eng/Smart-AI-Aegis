# 版本紀錄（半自動加工選單）

本檔為**版號與功能對照**之主紀錄；命名規則見下節。  
若由 **AI／代理** 接手本外掛，建議先讀 **`docs/行為準則.md`**（**§8.0～§8.4** 為孔加工模板之**完整書面契約**）與 **`docs/AI_閱讀順序.md`**，再回來讀本檔最上方新版章節。  
人類開發者若要快速還原「何時定了什麼、現在該以何為準」，可讀 **`docs/INTEGRATED_HANDOFF.md`**（含 04-25 ~ 05-02、回歸順序，以及對 **§8.4** 之交叉引用）；協作與變更門檻見 **`docs/行為準則.md`**（**§1**、**§8.0**）。  
**封存策略**：不再維護獨立 `版本紀錄_V*_*.md` 封存檔；**V1.03** 凍結敘述與後續補充請直接寫入本檔 **## V1.03（2026-05-01）** 及上方各版節。升版流程見 **`docs/VERSIONING.md`** §4。

---

---

## V2.0358（2026-05-23）

**+0.0001（細修 §2.1）**：**思考式 L2 加深**

| 模組 | 內容 |
|------|------|
| **`thinking_l2_plan.py`** | L2 資格須 **L0 + L1**；孔列 **identity 指紋**；Setup2 **重映射**；劇本 **JSON 持久化**（`memory/data/l2_plans/`） |
| **`thinking_programming.py`** | L2 路徑 **panel/2D 驗證**；`wcs_action` + **`confirm_flip`** 閘門；重掃失敗寫診斷 |
| **`intuitive_programming.py`** | `validate_ai_recommendations_for_execute` 共用驗證 |
| **面板 / MCP** | Setup2 帶 `confirm_flip`；`get_multi_setup_plan` 可讀磁碟劇本 |

- **程式同步**：**V2.0358** / manifest **2.0358.0** / `palette.html`。
- **文件**：**`docs/THINKING_PROGRAMMING.md`** 更新為 L0/L1/L2 **已實作**。

## V2.0357（2026-05-24）

**+0.0100（辨識／CAM §2）**：**flat_depths → 工序切深與 S/F 寫回**（顶面铣族深化 Phase 1）

| 模組 | 內容 |
|------|------|
| **`Smart_AI/reasoning/cam_depth_plan.py`** | `build_cam_depth_context`：顶面粗铣 `stock top → surface top`（**顶面胚料厚度**）；外轮廓深度；多階 `terrace_face_ops.face_depth` |
| **`smart_ai_cam_machining/cam_operation_tuning.py`** | execute 建工序後寫入 `topHeight`／`bottomHeight`／`maximumStepdown`、AI **rpm/feed** |
| **`execute._apply_2d`** | 套用 `camDepthContext`；多階台面帶 `terrace_spec` |
| **MCP** | **`get_cam_depth_plan`**、**`verify_cam_depth_plan`**；`scan_machining_features` 回傳 `cam_depth_context` |

- **程式同步**：**V2.0357** / manifest **2.0357.0** / `palette.html`。
- **驗收**：MCP `verify_cam_depth_plan`（需重載外掛）；实机 execute 日誌含 `[CAM深度]`、`[CAM參數]`。

## V2.0347（2026-05-23）

**累加自 V2.0316**（依 **`docs/VERSIONING.md`** §2／§2.1，**不**覆蓋小數段）：

| 步驟 | 刻度 | 顯示版號 | 內容 |
|------|------|----------|------|
| 基準 | — | V2.0316 | 穩定化細修（settings／3D Assembly／診斷／API 資料庫） |
| +0.0100 | 辨識 §2 | **V2.0326** | **Setup WCS 平面深度**（`flat_depths`）：`job_stockFixed*`、`surfaceZHigh/Low`、WCS Z0 深度語意；水平面 ±Z 法向 |
| +0.0100 | 辨識 §2 | **V2.0336** | **2D 轮廓**：顶面面铣几何（面積、**頂面胚料厚度**、除顶面坯料体积）；多階台面 `terrace_face_ops`；`contour_2d_recognizer` 帶入 |
| +0.0100 | 辨識 §2 | **V2.0346** | **3D 預覽 WCS** 對齊 mesh；思考式 **L1** 擴展（口袋 R、官方口袋面板）；`feature_catalog` 平面摘要 |
| +0.0001 | 細修 §2.1 | **V2.0347** | 標籤／面板佈局；Smart_AI import 修正；`palette` 共通設定橫向格線 |

- **程式同步**：`ADDIN_VERSION`、`.manifest` **2.0347.0**、`palette.html` **V2.0347**、`agent_manifest`、MCP 版號字串。
- **文件**：**`docs/CAM_FEATURE_RECOGNITION_MAP.md` §2.1**（平面深度／坯料 Z 契約）、本節、**`docs/開發對話與變更.md` ## 2026-05-23（二）**。

## V2.0316（2026-05-23）

- **解除顯示版號凍結**：升級為 **V2.0316**（manifest **2.0316.0**、`palette.html` **V2.0316**），同步今日所有重大優化與 bug 修正。
- **防止範本設定檔（`ui_defaults.json`）損毀**：將加工範本名稱與儲存實體路徑完全解耦，前端載入與套用只讀取設定值，徹底防範儲存設定時破壞資料夾路徑的 bug。
- **修復 3D 射線掃描裝配體黑邊/Bounds 錯誤**：將 3D 掃描的 fallback 實體蒐集器改為遞迴搜尋所有組件實體（`design.allComponents`），徹底解決在裝配體/組裝件模型上因 top-level bodies 為空而導致 3D 掃描點雲回傳 0 點、產生 xmin > xmax 包圍盒錯誤的問題。
- **修復草圖繪製按鈕凍結 bug**：修復了 `_send_vision_sketch_palette_result` 內部的靜默 `NameError: ui is not defined`。改用全域 `diagnostics` 與應用程式接口雙重 fallback 安全取得 UI 物件，確保在 Fusion 草圖繪製完成後前端按鈕立刻解凍、恢復為可點擊狀態。
- **診斷日誌自動載入**：優化 `_ensure_diag_palette` 邏輯，當發現 `holeProcessDiagPalette` 未被系統建立時，會自動且動態地依據 `diag_palette.html` 初始化該調色盤，完美解決診斷日誌點擊後沒有任何反應或無法開啟的異常。
- **排版與對齊美美化**：修正設定頁面核取方塊（Checkbox）被 CSS 錯誤拉伸為 120px 寬度的問題，優化說明文字折行對齊，使其恢復精美一致的暗色高質感界面。
- **整合 Fusion 360 官方 API 唯讀資料庫**：將使用者提供的 12 個 HTML 官方 API 參考手冊，進行亂碼修復（Mojibake Healing，徹底恢復為正常繁體中文）與去除 CSS 樣式雜訊，編譯為 AI 能高效精準讀取的唯讀 Markdown 資料庫：`docs/FUSION_API_DATABASE.md`。

## V2.0315（2026-05-10）

- **顯示版號凍結**：**`ADDIN_VERSION`／`.manifest`／`palette.html`** 固定為 **V2.0315**（**2.0315.0**）；此版起**同線小改不再遞增**顯示版號，直至使用者另行解除凍結。  
- **孔列合併**（**`mergeHoleListByDia`**）：合併鍵 **`(直徑字串, through, isCBLarge, depth_bucket)`**——**通孔／盲孔**與**沉頭大徑**分桶；通孔 **`depth_bucket`** 為 **`''`**（不依深度拆列）；盲孔 **`depth_bucket`** 為深度 **`round` 至小數一位**之字串，**僅同深度盲孔**併列。不依 **`type`**、**`dir`** 拆列。  
- **鑽孔模板刀具**：**`ENABLE_DRILL_LIBRARY_TOOL_MATCH`** 預設 **`False`**（見 V2.0313 條目說明）。  
- **編程模式**：**`docs/PROGRAMMING_MODES.md`** — **學習層**（概念／KnowledgeDB 持續）與 **使用層**（**直覺式**＝有限制、**思考式**＝開放式）分離；執行記錄含 **`programming_mode`**／**`usage_tier`**。  
- **直覺式編程**：**`intuitive_programming.py`**、**`programming_modes.py`**；MCP／面板見 **`docs/INTUITIVE_PROGRAMMING.md`**。  
- **思考式編程**：**`thinking_programming.py`** — **以直覺式為基底**；**L0** 與直覺式相同加工、標記 `thinking`；**L1/L2** 規劃加深。MCP **`check_thinking_eligibility`**／**`run_thinking_programming`**／**`get_thinking_layers`**。見 **`docs/THINKING_PROGRAMMING.md`**。  
- **一鍵直覺式（P0）**：MCP／面板 **`run_intuitive_one_click`**；**`run_internal_ai_autopilot`** 預設停用。驗收件型表 **`docs/INTUITIVE_VALIDATION_PARTS.md`**；**.f3z 學習**規劃 **`docs/F3Z_LEARNING.md`**。  
- **參考範本庫**：桌面 **`Fusion.lnk`** → **`E:\Fusion\參考範本`**（**`f3z已編程`**）；**`reference_paths.py`** + **`extract_f3z_from_shortcut.py`**。

## V2.0314（2026-05-10）

- **細修刻度**：**+0.0001**；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **孔列合併**：**`mergeHoleListByDia`** 改為僅以 **`(直徑字串, 通孔/盲孔)`** 分桶合併（不再依 `type`、`dir`、`depth`、`isCBLarge` 拆列），避免同徑多列重複套用鑽孔模板；盲孔合併時 **`depth`** 取數值較大者。

## V2.0313（2026-05-10）

- **細修刻度**：**+0.0001**；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **鑽孔模板刀具**：預設**不再**於 `_createOpFromTemplate` 內依本機庫換同徑 SG/HSS；新增 **`ENABLE_DRILL_LIBRARY_TOOL_MATCH`**（預設 **`False`**）。設 **`True`** 可恢復舊「庫刀優先」行為。

## 與程式同步（維護基準）

以下與 **`Smart_AI_CAM.py`**、**`.manifest`**、**`palette.html`** 實際內容對齊；**版號基準 V2.0357**。變更程式後請核對本表，並依 **`docs/VERSIONING.md`** §4 更新本表與版次章節。

| 項目 | 程式／檔案（摘要） |
|------|-------------------|
| **顯示版號** | `ADDIN_VERSION = 'V2.0357'` |
| **manifest** | `version` **2.0357.0**；`description` **Smart AI CAM V2.0357** |
| **面板預設字串** | `palette.html` → `<title>`、`#addin_version_badge` **V2.0357**；寬 **800**、高度 **900**（與主檔 `FIXED_PALETTE_WIDTH`、`palettes.add(..., 900)` 一致） |
| **CAM 切深計劃** | `cam_depth_plan.build_cam_depth_context` → execute `camDepthContext`；MCP **`verify_cam_depth_plan`** |
| **Setup WCS 平面深度** | `_scanFlatDepths()` → `flat_depths`（`job_stockFixedZOffset`、`surfaceZ*`、WCS Z0→底面）；見 **`docs/CAM_FEATURE_RECOGNITION_MAP.md` §2.1** |
| **2D 轮廓顶面** | `contour_2d_recognizer` → 面積、頂面胚料厚度、除顶面坯料体积（mm³） |
| **模板路徑鍵** | `TEMPLATE_FOLDER_PATHS`：`generalHole`、`tapHole`、`locatingHole`、`countersinkHole`、`slotHole`、`holeChamfer`、`contourChamfer`、`topFaceRough`／`topFaceFinish`／`topFaceLegacy`、`profileRough`／`profileFinish`／`profileLegacy`（全文對照 **`docs/行為準則.md` §8.1**） |
| **倒角白名單** | `ALLOWED_CHAMFER_TAGS = ('C0.2', 'C0.3')` |
| **大孔通孔預鑽** | `LARGE_HOLE_PILOT_THRESHOLD_MM = 6.5`；預鑽刀直徑 **`PILOT_DRILL_DIAMETER_MIN_MM`**～**`MAX_MM`** = **2.6～5.5** mm |
| **execute 尾段** | `ENABLE_GLOBAL_OP_REORDER_ON_EXECUTE`、`AUTO_GENERATE_TOOLPATH_ON_EXECUTE`、`REUSE_EXISTING_TEMPLATE_OPS`（預設 **`True`**） |
| **孔列合併（掃描後）** | **`mergeHoleListByDia`**：合併鍵 **`(直徑字串, through, isCBLarge, depth_bucket)`**（通孔 `depth_bucket` 為 `''`；盲孔為 **`round(depth_mm,1)`** 字串，僅同深度併列） |
| **鑽孔庫刀替換** | `ENABLE_DRILL_LIBRARY_TOOL_MATCH`（預設 **`False`**；**`True`** 時恢復套用模板後掃本機 SG/HSS 同徑換刀） |
| **長條孔／2D 內輪廓** | `SLOT_POCKET_TOOLPATH_INSIDE_SLOT` 及一組 `SLOT_*`；`CAM2D_INNER_CONTOUR_FLIP_ALL_CHAINS_AFTER_BIND`（`SLOT_FLIP_ALL_CHAIN_IS_REVERTED_AFTER_SLOT_BIND` 為其別名） |
| **槽列 UI 邏輯標** | `_slot_ui_logic_rev`（字串常數，供除錯／變更對照） |

**開發過程紀要**（含英文 notes）：**`docs/開發對話與變更.md`**（單檔連續維護；外掛根目錄無 `.md`）。

---

## V2.0310（2026-05-04）

- **版號語意（累加，非覆蓋）**：依 **`docs/VERSIONING.md`** §1 修訂——每新增一整條可跑通 **辨識→模板→工序→刀路** 之族，顯示數值 **+`1.0000`**；僅擴辨識 **+`0.01`**（小數第三位起之刻度語意不變）；細修 **+`0.0001`**。故自 **`V1.0310`** 確認**第二族（長條孔／槽）**端到端可用後，**`1.0310 + 1.0000 = 2.0310`**，顯示 **`V2.0310`**，`manifest` **`2.0310.0`**（**保留**原辨識段 **`.03`** 與細修尾碼 **`.0310`**，**不**改為 **`V2.0100`**）。  
- **整數 `2` 的意義**：除 **V1** 之**孔**族外，**槽**族已可穩定帶入模板並生成工序與預期刀路。  
- **文件（本日彙整）**：**`docs/開發對話與變更.md`**（當日改動與交叉索引）、**`docs/REF_2D內輪廓_loop_edges順序.md`**（**§0.0** **Fusion 工法的幾何依據**（輸入／判斷／結果、特徵對照表、辨識器／工法分工）；**§0** Fusion 設計思路；**§0.1** **`is_inner_loop`**；**§2** **`reversed()`**／**`isReverted`**；**暫定參考**）、**`docs/行為準則.md` §8.5**、**`docs/INTEGRATED_HANDOFF.md`**（§2 **2026-05-04**）、**`docs/AI_閱讀順序.md`**、**`docs/VERSIONING.md`** §7。  
- **封存註**：不另建獨立封存檔；細節已反映於本檔對應版節。

## V1.0310（2026-05-04）

- **細修刻度**：**+0.0001**（`1.0309`→`1.0310`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **2D 內輪廓綁定後處理**：主開關更名 **`CAM2D_INNER_CONTOUR_FLIP_ALL_CHAINS_AFTER_BIND`**（預設仍 **`True`**）；**`SLOT_FLIP_ALL_CHAIN_IS_REVERTED_AFTER_SLOT_BIND`** 改為**相容別名**（同一布林值）。註解與 **`_slot_flip_all_chain_is_reverted_in_cad2d`** docstring 改寫為 **「2D 輪廓工序／內輪廓加工」** 語意（長條孔 execute 為首測路徑），供往後其它內輪廓 CadContours2d 綁定沿用。  
- **封存註**：不另建獨立封存檔；細節已反映於本檔對應版節。

## V1.0309（2026-05-03）

- **細修刻度**：**+0.0001**（`1.0308`→`1.0309`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **長條孔 execute 修正**：面板經 JSON 回傳的 **`slotUrl`／`chamferUrl`** 為 **`_template_cache_key` 字串**，先前直接傳入 **`CAMTemplateLibrary.templateAtURL`** 會觸發型別錯誤（需 **`adsk.core.URL`**）。新增 **`_slot_palette_url_key_to_library_url`**，自 **`allSlotMap`／`allSlotChamferMap`** 還原本機 **`url`** 物件再載入模板；無對應時改為可讀訊息。  
- **封存註**：不另建獨立封存檔；細節已反映於本檔對應版節。

## V1.0308（2026-05-03）

- **細修刻度**：**+0.0001**（`1.0307`→`1.0308`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **長條孔 execute 幾何**：槽辨識 `faces` 含開口平面與槽壁側面等，**`bind_all_faces=True`** 時易將**法向垂直於 Setup WCS Z 的大側壁**一併送入 **pocket2d**，導致 **NoToolpath**。新增 **`_filter_slot_opening_planar_faces`**：僅保留 **\|n·z_axis\| > 0.9** 之**平面**；若有 **`face_z_wcs_mm`**（`slot_features` 由 `_buildSlotData` 附帶）則選 bbox 沿 Z 投影中點**最接近辨識開口 Z** 者；無候選時**回退**原 `faces` 並寫 **`[slot-bind]`** 診斷。  
- **文件**：補充條目已寫入本檔對應版節（不另建封存檔）。

## V1.0307（2026-05-03）

- **細修刻度**：**+0.0001**（`1.0306`→`1.0307`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **診斷輸出（孔／槽除錯）**：**`[hole-debug]`**／**`[slot-debug]`** 總覽與逐列 JSON 欄位改為中文標籤；**來源**（掃描／面板開關／重建／全量重掃）與槽除錯之**邏輯版／槽列數／分組數**等一併中文化；**`_diag_source_zh`** 孔／槽共用。  
- **槽掃描列**：無 **`slot_dict_for_log`** 時仍輸出前 16 筆 **`slotInfoList`** 之中文化摘要；**`faces`** 僅輸出**面數**。  
- **可讀性**：布林欄位（貫穿、沉頭、加工用啟用等）於診斷 JSON 顯示為 **`是`／`否`**；**坯頂／坯底 Z** 用字修正（原誤「胚」）。

## V1.0306（2026-05-03）

- **細修刻度**：**+0.0001**（`1.0305`→`1.0306`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **「重新快取模板」可觀測性**：按鍵改送 **`rescan` + `reason=template_cache`**（相容舊模組快取）；**`ui/palette_controller.py`** 於 **`rescan`** 入口寫入 **`[template-cache]`** 訊息到診斷與 **Text Commands**，避免使用者只看到 **`slot-debug`**／**`hole-debug`** 而誤判按鍵無效。  
- **相容**：仍保留 **`_refreshTemplateCacheForPalette()`** 供未來／手動呼叫；主 UI 路徑以 **`rescan`** 為準。

## V1.0305（2026-05-03）

- **細修刻度**：**+0.0001**（`1.0304`→`1.0305`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **調整選單新增按鍵**：程序診斷分頁新增 **「重新快取模板」**，前端觸發 `refresh_template_cache` action。  
- **後端重建流程**：新增 **`_refreshTemplateCacheForPalette()`**（失效 `template_fs_cache`、重建模板映射與 2D 模板池、刷新孔表與 init 資料）；`ui/palette_controller.py` 增加 action handler 與狀態回報。  
- **文件**：封存檔「補充同步」與版本對照已更新。

## V1.0304（2026-05-02）

- **細修刻度**：**+0.0001**（`1.0303`→`1.0304`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **模板載入可觀測性**：**`templates/template_service.py`** 之 **`build_template_maps`** 在**嚴格指定資料夾**與**相容整樹後備**（**`_scan_template_tree`**／**`_scan_chamfer_tree_only`**）分支輸出 **`[template-maps][strict]`**／**`[template-maps][compat]`** 訊息；主檔 **`run()`** 註冊 **`set_template_maps_diag_log`**，寫入診斷面板（若已開）及 **Text Commands**，對照 **`docs/行為準則.md` §8.0.1**。  
- **文件**：**§8.0.1** 已補「可觀測性」一句。

## V1.0303（2026-05-02）

- **細修刻度**：**+0.0001**（`1.0302`→`1.0303`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **本機模板索引快取**：新增 **`templates/template_fs_cache.py`**，優先掃 **`%AppData%\Roaming\Autodesk\CAM360\templates\{材質}`** 下所有 **`.f3dhsm-template`**（略過 **`_XRef_`**），以 **`relpath + mtime` 簽章**快取；**`collect_assets_from_folder_path`**、**`collect_slot_chamfer_assets`**（**`template_service.py`**）先依索引篩 **`TEMPLATE_FOLDER_PATHS`** 對應子樹，再以 **`templateLibrary.join`** 還原 Fusion **`url`**（無檔或 **`join` 失敗**時**後備**原 **`childAssetURLs`／`_scan_folder_slot_chamfer`**）。**`template_folder_exists`** 先檢本機資料夾再以 API 核對。  
- **失效時機**：**`_fullRescanForPalette`** 與面板啟動時「僅重載長條孔／輪廓倒角」分支呼叫 **`template_fs_cache.invalidate_all()`**，避免舊記憶體快照與磁碟脫節（與先前 **`allDrillMap` 已存在即跳過整段載入** 之問題分開處理：索引層仍會在失效後重掃）。  
- **文件**：**`docs/行為準則.md`** **§8.0、§8.2、§8.4.1** 已補索引與契約邊界；**`docs/VERSIONING.md`**（§4 升版清單）、**`docs/AI_閱讀順序.md`**、**`docs/INTEGRATED_HANDOFF.md`** 已對照本版；**`docs/CHECKLIST.md`** 已新增 **§12** 回歸項；**V1.0301～V1.0303** 摘要已反映於本檔各版節。

## V1.0302（2026-05-02）

- **細修刻度**：**+0.0001**（`1.0301`→`1.0302`）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。  
- **孔倒角載入（行為變更）**：嚴格模式（已自 **`generalHole`／`tapHole`／`locatingHole`** 載入鑽模板）下，**`chamferMap`** 改為**只**自 **`TEMPLATE_FOLDER_PATHS["holeChamfer"]`** 即 **`{material}/倒角刀模塊 【{material}】/孔倒角 【{material}】`** 遞迴蒐集，**不再**掃描整個 **`倒角刀模塊 【{material}】`** 樹，避免日後於模塊內新增其他子資料夾、檔名同為 **`D10.0 …`** 時與孔邊倒角模板混淆。  
- **常數與檢查**：主檔新增 **`holeChamfer`** 鍵；**`validate_configured_template_paths`** 之 **`check_keys`** 納入 **`holeChamfer`**，缺資料夾時啟動提示。若無 **`holeChamfer`** 字串（相容／誤刪鍵），嚴格模式**後備**仍走舊之 **`_scan_chamfer_tree_only`**。  
- **兩材質庫結構**：**`AL6061`** 與 **`S50C`** 之 **`holeChamfer`** 階層**互為同一套結構**，僅材質字串不同；兩根路徑皆須實際存在並擺放對應模板（見 **`docs/行為準則.md` §8.2**「兩材質目錄對稱」）。  
- **相容**：三個嚴格鑽資料夾皆空時，仍 **`_scan_template_tree`**（整樹倒角後備）。  
- **文件**：**`docs/行為準則.md`** **§8.0、§8.1 表、§8.2、§8.4.3** 已與本行為對齊；**§8.4** 索引表（V1.0301 小節）中 **§8.2／§8.4.3** 敘述以 **V1.0302** 程式為準。

## V1.0301（2026-05-02）

- **細修刻度**：見 `docs/VERSIONING.md` 第 2.1 節，細微修正步長為 **+0.0001**（顯示四位小數，例 **`V1.0301`**）；`ADDIN_VERSION`、`.manifest`、`palette.html` 已同步。整數與辨識刻度語意仍依 `docs/VERSIONING.md` 第 1、2 節。
- **行為準則（總覽）**：**`docs/行為準則.md`**（協作與產品原則；**更動原程式碼前須先說明影響並取得同意**）；**§3** 併入 **`docs/BASELINE_ALIGNMENT.md`**／**`docs/INTEGRATED_HANDOFF.md`** 之不可擅自更動項；並補 **§3.6～3.7**（UTF-8／職責）、**§2**（§5 四段式與 MCP 驗收界限）、**§7**（版號與 `版本紀錄` 最新在上）、**§8～9**（模板路徑、槽孔層級、UI 正反面／沉頭、辨識模組摘要）。已鏈入 `docs/AI_閱讀順序.md`、`docs/VERSIONING.md` §7、`docs/INTEGRATED_HANDOFF.md` §0、`docs/版本紀錄.md` 開頭與 `docs/開發對話與變更.md` 索引。

### V1.0301 — 孔加工模板「完整契約」文件化（**`docs/行為準則.md` §8.0～§8.4**）

以下為**同一輪定案**之補充紀錄：程式版號仍 **V1.0301**，**以 `docs/行為準則.md` 正文為唯一全文**；本節為**索引式完整摘要**，供版次追溯與 AI 檢索。

| 小節 | 主題 | 要點（實作錨點） |
|------|------|------------------|
| **§8.0** | 防擅自更動 | 明列變更前**必**依 **§1** 之符號集合：`TEMPLATE_FOLDER_PATHS`（含 **`holeChamfer`**）、`ALLOWED_CHAMFER_TAGS`、`template_service.build_template_maps`（含 `strict_keys`、`add_to_drill_map`／`add_to_chamfer_map`、嚴格載入後**僅** **`holeChamfer`** 路徑蒐集孔倒角；後備 **`_scan_chamfer_tree_only`**）、主檔 **`buildDropItems`** 等。程式與文件衝突時**不得**由 AI 單方改碼「對齊猜測」。 |
| **§8.1** | 路徑鍵表 | **`TEMPLATE_FOLDER_PATHS`** 全鍵與本機樣板庫相對階層字串（與主檔常數**逐字**對照；含 **`holeChamfer`**）；一鍵一類、不混用；`validate_configured_template_paths` 之 `check_keys` 註記。 |
| **§8.2** | 孔倒角來源 | **V1.0302 起**：鍵 **`holeChamfer`**；嚴格載入後**只**蒐集 **`…/孔倒角 【{material}】`** 入 **`chamferMap`**（見 **`docs/版本紀錄.md` V1.0302**）。**`contourChamfer`** 僅槽孔；**`strict_loaded=False`** 仍 **`_scan_template_tree`**。 |
| **§8.3** | 概要 | 資料流（**`buildTemplateMaps` → `buildDropItems`**）、執行（**`drillUrl`／`chamferUrl`**）、槽孔／沉頭大孔列分流；**細部不以此節為準**。 |
| **§8.4.1** | 錨點表 | 路徑常數、白名單、**`template_service.build_template_maps`**、**`buildTemplateMaps`**、**`buildDropItems`** 及輔助函式之檔案對照。 |
| **§8.4.2** | **`drillMap`** | **`strict_keys = ("generalHole", "tapHole", "locatingHole")`** 順序掃描；任一路徑有資產即 **`strict_loaded`**；**`add_to_drill_map`**：槽孔關鍵字跳過（**長條孔、槽孔、slot、obround、racetrack** 等與程式一致）、沉頭／countersink 標記、鑽／絞／搪關鍵字、**牙孔直徑 0.1 mm 展開**、非牙 **`D` 檔名解析**；fallback **`_scan_template_tree`**。 |
| **§8.4.3** | **`chamferMap`** | **V1.0302 起**：嚴格模式 **`collect_assets_from_folder_path`** 僅 **`holeChamfer`**；**`add_to_chamfer_map`** 規則同上；後備 **`_scan_chamfer_tree_only`**；**`contourChamfer`** 不經本池。 |
| **§8.4.4** | 直徑模糊對表 | **`_drill_items_by_target_dia`**／**`_chamfer_items_by_target_dia`**：先精確鍵，否則最小差鍵且 **差 ≤ 0.2 mm**；**0.2 為契約數值**。 |
| **§8.4.5** | 倒角白名單 | **`ALLOWED_CHAMFER_TAGS = ('C0.2', 'C0.3')`**；**`_is_allowed_chamfer_name`**（大寫、去空白後子字串匹配）。 |
| **§8.4.6** | **`buildDropItems`** | 共同前處理；**A 一般列**：先剔除沉頭鑽模板再 **鑽×倒角 → 僅倒角 → 僅鑽 → (不使用)**（**順序為契約**）；**B `isCBLarge`**：**`cs_drill`／`source`** 定義與 **組合 → 僅鑽 → 僅倒角 → (不使用)** 後 **`return`**。 |
| **§8.4.7** | 執行與槽孔 | **`None` URL** 不建該類工序；槽孔用 **`slotHole`＋`contourChamfer`** 與專用 UI，**不**用 A 段組合語意。 |
| **§3.3** | Baseline 交叉 | 等價函式列表中 **`buildTemplateMaps`／`buildDropItems`** 指向 **§8.4**。 |

- **`docs/INTEGRATED_HANDOFF.md` 同步**：**§0** 表格、`§2`（05-02）模板路徑子項、**§3** `buildDropItems` 抽離註記、**§10.5** 與主孔表鑽／倒角下拉之關係，皆已指向 **`docs/行為準則.md` §8.0～§8.4**（以 **§8.4** 為技術全文、`§8.0` 為變更門檻）。

---

## 紀錄檔命名定義

| 類型 | 檔名格式 | 說明 |
|------|----------|------|
| **彙總檔（本檔）** | `docs/版本紀錄.md` | 所有版號列於此，**新版章節一律插在檔案最上方**（最新在上）。 |
| **歷史封存** | （已廢止獨立封存檔） | 凍結敘述請直接寫入本檔 **## V1.03（2026-05-01）** 與上方版節，避免平行檔漂移。 |

**建議**：每次改 `ADDIN_VERSION` 時，至少更新本檔一節；若該次要交付給他人，可再複製一份符合上表格式的封存檔。

---

## 開發對話變更摘要（2026-05-04）

- **版號累加、MD 交叉索引、2D 內輪廓 `loop_edges` 暫定參考**：見 **`docs/開發對話與變更.md`**、**`docs/REF_2D內輪廓_loop_edges順序.md`**。

## 開發對話變更摘要（2026-05-02）

- **完整條列（含預鑽、CAM 安全、刀路／排序、訊息、未採納優化與 MCP 抽檢）**：見 **`docs/開發對話與變更.md`**。
- **英文同日紀要**：**`docs/開發對話與變更.md`**（根目錄舊路徑僅索引）。
- 本節僅作索引；版號仍以 `ADDIN_VERSION` 與下方正式版節為準。

---

## V1.03（2026-05-01）

### 版號語意

| 欄位 | 本版取值 | 意義 |
|------|----------|------|
| **整數** | `1` | 已有 **1** 條穩定管線：**孔辨識** → 孔相關模板工序 → 可觸發並完成刀路生成。 |
| **小數兩位** | `03` | 辨識範圍刻度；本版可辨識並納入加工選單邏輯者包含：**通孔與盲孔**、**沉頭孔**、**長條孔**（三項辨識能力對應至 `.03`）。 |

### 功能與辨識範圍

1. **孔辨識**：可將結果帶入**孔工序（模板）**並**生成刀路**（對應整數 `1`）。
2. **辨識範圍**（對應小數 `.03`）：**通孔／盲孔**、**沉頭孔**、**長條孔**。

### 相關檔案

- 版號規則全文：`docs/VERSIONING.md`
- 程式常數：`半自動加工選單【UI穩定版】.py` 內 `ADDIN_VERSION`

---
## 融合辨識（孔）— 2026-05-01 實作

- `recognizers/hole_recognizer.py`：`scan_holes_by_ray` 先蒐集 **RecognizedPocket** 種子列，再與 **圓柱+射線（custom-ray）** 以 (XY容差位置, 直徑) 合併；同鍵以 custom-ray 為準。
- 沉頭定義：同 XY 雙孔、直徑差 ≥ 2mm、拓樸 _is_counterbore_topology 成立時：小孔強制 **通** 且不標沉頭；大孔 **盲** 且 is_countersink_large=True。
- 孔深≈板厚強制通孔：**不套用**於 is_countersink_large 列（避免 D10 類沉頭大孔被翻成通孔）。
- 半自動加工選單【UI穩定版】.py：_scanAndBuildHoleInfo 將 is_countersink_* 映射到 isCBLarge / isCBSmall。

---

## V1.03 後續更新（2026-05-02，UI/模板穩定化）

- 對話與變更完整紀錄已建立：`docs/開發對話與變更.md`
- 本輪重點：
  - 設定值套用/重掃/存預設一致性修正
  - 模板路徑嚴格分流（一般孔/牙孔/定位孔 與 沉頭孔/長條孔隔離）
  - 缺模板警告改為「每直徑」可讀提示
  - 沉頭列 UI 顯示邏輯穩定化（避免鑽深欄位干擾）
  - pitch 預設值來源改為模板參數優先（含 `pitchMM`）

